describe("useOrders", () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it("should initialize with correct default values", () => {
    // const { result } = renderHook(() => useOrders("open"));
    // expect(result.current.orders).toBeUndefined();
    // expect(result.current.isLoading).toBe(false);
    // expect(result.current.selectedStatus).toBe("open");
    // expect(result.current.openOrdersCount).toBeDefined();
    // expect(result.current.validOrdersCount).toBeDefined();
    // expect(result.current.readyOrdersCount).toBeDefined();
    // expect(result.current.isAllOrdersSelected).toBe(false);
    // expect(result.current.isSomeOrdersSelected).toBe(false);
  });
});
